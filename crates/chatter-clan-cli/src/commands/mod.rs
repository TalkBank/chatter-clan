//! Command handlers for the standalone `chatter-clan` binary.
//!
//! `clan` dispatches a parsed `ClanCommands` to the `talkbank-clan` analysis
//! engine, recovered verbatim from the chatter monorepo's `chatter clan`
//! subcommand.

pub mod clan;
